using System;
using System.Collections.Generic;
using System.Drawing;

namespace DynamicDraw
{
    /// <summary>
    /// Identifies the themes (which in turn decide the colors for each theme slot to use).
    /// </summary>
    public enum ThemeName
    {
        Light,
        Dark
    }
}
